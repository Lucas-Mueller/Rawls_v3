# Legacy Test Artifacts

This directory archives legacy test suites, templates, and `.bak` files that are no longer
executed by default pytest discovery (see `pytest.ini -> norecursedirs`). They are retained for
historical reference and can be restored or ported to the new pytest infrastructure if needed.
